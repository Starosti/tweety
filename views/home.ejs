<!DOCTYPE html>
<html lang="en">
  <%- include("head") %>
  <body>
    <%- include("header") %>
    <main>
      <div class="topBar">
        <div class="postTweet">
          <h2>Post a tweet:</h2>
          <form action="/tweet" method="post">
            <input
              type="text"
              name="user"
              id="user"
              placeholder="Username"
              required
            />
            <input
              type="text"
              name="content"
              id="content"
              placeholder="Tweet"
              required
            />
            <input type="submit" value="Submit" />
          </form>
        </div>
        <div class="searchTweet">
          <h2>Find a Tweet with ID:</h2>
          <form action="/tweet" method="get">
            <input type="text" name="id" placeholder="Tweet ID" />
            <input type="submit" value="Submit" />
          </form>
        </div>
      </div>
      <% if (tweets[0]) { %>
      <h2>All tweets:</h2>
      <% for (tweet of tweets) { %> <%- include("tweet",tweet) %> <% } %> <% }
      else { %>
      <h2>No tweet has been found!</h2>
      <% } %>
    </main>
  </body>
</html>
